// JavaScript file. The rule targets TypeScript-only syntax, so it never fires
// here -- not even on a non-empty annotationless literal that would be reported
// in a `.ts` file.
const obj = { a: 1 };
